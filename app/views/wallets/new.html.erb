<!DOCTYPE html>
<html>
  <%= stylesheet_link_tag "new_wallet.css" %>
  <head>
    <title>Nowy portfel</title>
    <link href='http://fonts.googleapis.com/css?family=Poppins&subset=latin,latin-ext' rel='stylesheet' type='text/css'>
    <link rel="icon" type="image/png" sizes="32x32" href="favicon-32x32.png">
  </head>
  <body>
    <div>
      <%= image_tag "Wersja5.png", class: "logo" %>
    </div>
    <div class="box">
      <h2>Dodaj Portfel dla: "<%= @user.nick %>"</h2>
      <div>
        <%=form_with model: [@wallet] do |form| %>
          <div class="field">
            <ion-icon name="pricetag-outline"></ion-icon>
            <%= form.label :name, class: "etykieta" %>
            <%= form.text_field :name, class: "okno" %>
          </div>
        <% @wallet.errors.full_messages_for(:title).each do |message| %>
        <div><%= message %></div>
        <% end %>
      </div>
      <div>
        <%= form.submit 'Stwórz', class: "guzik" %>
        <% end %>
      </div>
    </div>
    <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>
  </body>
</html>